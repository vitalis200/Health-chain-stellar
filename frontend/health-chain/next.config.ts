import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* * Removing the 'turbo' and 'skipProxyUrlNormalize' keys as your 
   * current version is reporting them as unrecognized. 
   * Next.js will use its defaults which are safer.
   */
};

export default nextConfig;